[Unit]
Description=ODIN: 面板就绪后重新 probe 触摸屏（FT8716 是 TDDI）
#
# FT8716 与显示共用一颗 TDDI，面板没上电它就不应答 I2C。而 msm(DRM) 和
# panel_ft8716 都是模块、靠 udev 冷插加载，实测比触摸（edt_ft5x06）晚约 5 秒
# 就绪 —— 于是触摸先 probe，拿到 -5(EIO) 就再也不重试，整个会话没触摸。
# 完整的时间线与"为什么不在 DT 里补 vcc-supply"见
# /usr/local/sbin/odin-touchscreen.sh 的头部注释。
#
# ⚠️ 这里**不能**写 After/Wants=dev-dri-card0.device。
#    2026-09-08 实测：那个 device unit 在这台机器上**从来不激活**
#    （systemctl show 出来 ActiveState=inactive、ActiveEnterTimestamp 为空），
#    尽管 /dev/dri/card0 节点本身是存在的（device unit 由 udev 事件创建，这里没成）。
#    于是 systemd 一直等它，等到 DefaultDeviceTimeout(90s) 才放行 —— 用户态启动
#    时间直接被顶到 1min35s（内核其实只要 7.6s）。
#    正确做法：不依赖 device unit，由脚本自己轮询 /dev/dri/card0 是否真的出现
#    （见 odin-touchscreen.sh），那才是"显示起来了"的可靠信号。
After=systemd-modules-load.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/local/sbin/odin-touchscreen.sh

[Install]
WantedBy=multi-user.target
